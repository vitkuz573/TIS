<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Train Information System" Height="350" Width="525">
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBox x:Name="numberTextBox" Grid.Row="0" Margin="10" />
            <TextBox x:Name="cityTextBox" Grid.Row="1" Margin="10" />
            <DatePicker x:Name="departureDatePicker" Grid.Row="2" Margin="10" />

            <Button Content="Добавить" Click="AddTrainButton_Click" Grid.Row="3" Margin="10" />
        </Grid>

        <Canvas x:Name="canvas" Margin="10,10,10,10" Grid.Column="1" />
    </Grid>
</Window>
